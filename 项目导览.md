# Pi 项目导览

> 用途：帮助新加入的开发者或新开的 AI 对话快速建立项目上下文。
>
> 本文基于提交 `24bace27cf308c89707cf8005b4795d873e23f17`（2026-07-23）梳理。代码持续演进时，应以当前源码和 [`AGENTS.md`](AGENTS.md) 为准。

## 1. 一句话定位

Pi 是一套以“核心尽量小、能力通过扩展组合”为原则的 TypeScript Agent Harness monorepo。

它同时提供：

- 统一的多模型供应商调用层 `pi-ai`
- 带工具调用、流式事件和状态管理的低层 Agent 运行时 `pi-agent-core`
- 可交互的终端编码 Agent `pi-coding-agent`
- 独立的终端 UI 框架 `pi-tui`
- 面向新 Agent Harness 的通用会话与 SQLite 持久化能力
- 实验性的多实例 Server/RPC 管理层

Pi 不把所有常见 Agent 功能内置进核心。MCP、子 Agent、权限弹窗、计划模式、待办事项和后台 Bash 都不是内建能力；需要通过扩展、Pi package 或外部工具实现。

## 2. 最重要的架构边界

当前仓库里并行存在两条架构线，阅读和修改前必须先判断目标属于哪一条。

### 2.1 当前 `pi` CLI 的实际运行线

```text
pi CLI
  └─ AgentSessionRuntime
      ├─ SettingsManager
      ├─ ModelRuntime
      ├─ DefaultResourceLoader
      ├─ SessionManager（JSONL 会话）
      └─ AgentSession
          ├─ ExtensionRunner
          └─ Agent
              └─ agent-loop
                  └─ pi-ai Provider
```

当前 CLI 使用：

- [`packages/coding-agent/src/main.ts`](packages/coding-agent/src/main.ts)
- [`packages/coding-agent/src/core/agent-session-runtime.ts`](packages/coding-agent/src/core/agent-session-runtime.ts)
- [`packages/coding-agent/src/core/agent-session.ts`](packages/coding-agent/src/core/agent-session.ts)
- [`packages/coding-agent/src/core/session-manager.ts`](packages/coding-agent/src/core/session-manager.ts)
- [`packages/agent/src/agent.ts`](packages/agent/src/agent.ts)
- [`packages/agent/src/agent-loop.ts`](packages/agent/src/agent-loop.ts)

### 2.2 正在演进的新通用 Harness 线

`packages/agent/src/harness/` 内还有一套更通用的 `AgentHarness + Session + SessionRepo/Storage`：

- 将会话持久化、运行时快照、结构化操作锁、工具上下文和压缩放进通用 Harness
- 支持内存和 JSONL repo/storage
- `packages/storage/sqlite-node` 提供独立 SQLite 后端
- 目标是逐步承接当前 `AgentSession` 中的编排职责

关键事实：当前 `pi-coding-agent` 没有实例化 `AgentHarness`，也没有使用 SQLite 会话后端。不要把 [`packages/agent/docs/agent-harness.md`](packages/agent/docs/agent-harness.md) 中的规划项误认为 CLI 已有行为。

## 3. Package 地图

| Package | 职责 | 主要依赖 | 当前地位 |
|---|---|---|---|
| `@earendil-works/pi-ai` | 模型、Provider、认证、流式协议、消息和工具 schema | 外部模型 SDK | 最底层模型抽象 |
| `@earendil-works/pi-agent-core` | Agent 循环、状态、事件、工具执行；另含新 Harness | `pi-ai` | CLI 的低层运行时 |
| `@earendil-works/pi-tui` | 终端组件、编辑器、差量渲染、图片、输入和覆盖层 | `marked` 等 | 独立 UI 基础设施 |
| `@earendil-works/pi-coding-agent` | `pi` CLI、会话、扩展、资源、编码工具和运行模式 | `pi-ai`、`pi-agent-core`、`pi-tui` | 主产品 |
| `@earendil-works/pi-storage-sqlite-node` | 新 Harness Session 的 Node SQLite repo/storage | `pi-agent-core`、`pi-ai` | 新架构持久化后端 |
| `@earendil-works/pi-server` | 管理多个 RPC 子进程和本地 IPC，带可选 Radius 接入 | `pi-coding-agent` | 实验性 |

依赖方向：

```text
pi-ai ───────────────┐
  │                  │
  ▼                  │
pi-agent-core        │
  │                  │
  ├─► storage/sqlite-node
  │
  └──────────────┐
                 ▼
pi-tui ─────► pi-coding-agent ─────► pi-server
```

## 4. 一次 CLI 启动发生什么

入口是 [`packages/coding-agent/src/cli.ts`](packages/coding-agent/src/cli.ts)，主要编排在 [`main.ts`](packages/coding-agent/src/main.ts)。

启动顺序大致如下：

1. 解析参数，处理安装、更新、配置、导出和帮助等一次性命令。
2. 根据参数和终端状态选择运行模式：
   - `interactive`：完整 TUI
   - `print`：文本输出
   - `json`：结构化事件流
   - `rpc`：stdin/stdout JSONL RPC
3. 创建或打开 `SessionManager`，确定真正的会话工作目录。
4. 判断项目是否可信。项目本地扩展、package 和部分资源只在信任后加载。
5. 为最终工作目录创建一组绑定服务：
   - `SettingsManager`
   - `ModelRuntime`
   - `DefaultResourceLoader`
6. 资源加载器发现扩展、Skills、提示词模板、主题、`AGENTS.md`/`CLAUDE.md` 和系统提示词。
7. 扩展注册的 Provider 被写入 `ModelRuntime`。
8. `createAgentSession()` 创建 `Agent`，再用 `AgentSession` 包装它。
9. `AgentSessionRuntime` 持有当前 Session 和工作目录绑定服务，负责 `/new`、`/resume`、`/fork` 等会话替换。
10. 进入对应的交互、打印或 RPC 模式。

会话切换可能改变工作目录，因此设置、模型、扩展和资源会围绕新目录整体重建，而不是继续复用旧目录的运行时。

## 5. 一次用户请求的执行链路

### 5.1 `AgentSession` 前置处理

`AgentSession.prompt()` 依次执行：

1. 匹配扩展命令；命中后直接执行，不进入模型。
2. 发出 `input` 扩展事件，允许拦截、修改或完全处理输入。
3. 展开 `/skill:name` 和提示词模板。
4. 若 Agent 正在运行，根据调用方指定进入 steering 或 follow-up 队列。
5. 检查当前模型和认证。
6. 必要时在发送新请求前执行上下文压缩。
7. 发出 `before_agent_start`，允许扩展注入消息或修改本轮系统提示词。
8. 调用低层 `Agent.prompt()`。

### 5.2 低层 Agent 循环

[`agent-loop.ts`](packages/agent/src/agent-loop.ts) 的核心循环是：

```text
AgentMessage[]
  ├─ transformContext()       扩展可调整上下文
  ├─ convertToLlm()           过滤/转换应用消息
  ▼
pi-ai Context
  ▼
Provider.streamSimple()
  ▼
流式 AssistantMessage
  ├─ 无工具调用 ──────────────► 结束
  └─ 有工具调用
      ├─ 查找工具并准备参数
      ├─ TypeBox schema 校验
      ├─ beforeToolCall / tool_call，可阻止执行
      ├─ 执行工具
      ├─ afterToolCall / tool_result，可改写结果
      ├─ 生成 ToolResultMessage
      └─ 带工具结果进入下一轮模型调用
```

多个工具调用默认并行执行。若全局配置为 `sequential`，或批次中任一工具声明 `executionMode: "sequential"`，整个批次顺序执行。并行执行时，完成事件按实际完成顺序发出，但写入上下文的工具结果仍保持模型原始调用顺序。

每轮结束后依次检查：

1. 是否应在当前轮后停止
2. steering 消息
3. 剩余工具调用
4. follow-up 消息

### 5.3 `AgentSession` 后处理

`AgentSession` 订阅所有低层事件，并负责：

- 转发扩展生命周期事件
- 将消息追加到当前 JSONL Session
- 更新模型、思考级别、费用和上下文统计
- 对可重试供应商错误执行指数退避
- 对阈值或溢出的上下文执行自动压缩
- 清空或继续 queued message
- 最终发出 `agent_settled`

`agent_end` 只表示本次低层 Agent 循环结束；`agent_settled` 才表示重试、压缩和排队续跑也已处理完。

## 6. 消息、事件和队列

### 6.1 消息分层

`pi-ai` 只理解三种模型消息：

- `user`
- `assistant`
- `toolResult`

`pi-agent-core` 的 `AgentMessage` 可以通过声明合并增加应用消息。`pi-coding-agent` 额外使用：

- `bashExecution`
- `custom`
- `branchSummary`
- `compactionSummary`

进入模型前，`convertToLlm()` 会过滤或转换这些应用消息。

### 6.2 核心事件

典型事件顺序：

```text
agent_start
  turn_start
    message_start / message_update / message_end
    tool_execution_start / update / end
  turn_end
agent_end
agent_settled（由 AgentSession 追加）
```

### 6.3 Steering 与 follow-up

- steering：当前 assistant turn 和已启动的工具批次完成后，在下一次模型调用前插入。
- follow-up：Agent 原本准备停止，且没有工具和 steering 消息时才插入。
- 两类队列都支持 `one-at-a-time` 和 `all` 两种 drain 模式。

## 7. 模型与 Provider

### 7.1 `pi-ai` 的抽象

[`packages/ai/src/models.ts`](packages/ai/src/models.ts) 定义了核心对象：

- `Provider`：拥有 ID、认证方式、模型列表和流式实现
- `Models`：Provider 集合，负责认证解析、模型刷新和请求委派
- `Model`：具体模型元数据，包括 API 类型、上下文窗口、输出上限、成本和推理能力
- `Context`：系统提示词、消息和工具

Provider 可以：

- 使用静态模型目录
- 动态刷新模型并写入 `ModelsStore`
- 按认证凭据过滤可用模型
- 支持 API key、OAuth 或环境/运行时凭据
- 通过统一 `streamSimple()` 接受跨供应商请求
- 通过 `stream()` 接受供应商特定选项

### 7.2 CLI 中的 `ModelRuntime`

[`packages/coding-agent/src/core/model-runtime.ts`](packages/coding-agent/src/core/model-runtime.ts) 是 CLI 的规范模型运行时。它组合：

1. `pi-ai` 内置 Provider
2. `models.json` 配置覆盖
3. 扩展注册的 Provider 或原生 Provider
4. 文件凭据和仅当前进程有效的凭据
5. 本地/远程模型目录缓存

`ModelRegistry` 只是为扩展保留的同步兼容 facade；CLI 内部应直接使用 `ModelRuntime`。

## 8. 工具系统

内置工具定义位于 [`packages/coding-agent/src/core/tools/`](packages/coding-agent/src/core/tools/)。

仓库提供七种内置工具：

- `read`
- `bash`
- `edit`
- `write`
- `grep`
- `find`
- `ls`

默认激活前四种；`grep/find/ls` 通常用于只读工具配置。扩展注册的工具默认也会加入当前 Session。

工具由 TypeBox schema 描述输入。执行前统一校验，失败通过 `isError: true` 的 `ToolResultMessage` 返回模型。工具可流式上报部分结果，也可提供 TUI 和 HTML 渲染器。

Pi 本身没有权限沙箱。普通运行时，工具拥有启动 `pi` 的用户和进程权限。项目信任只控制项目本地资源是否加载，不等于进程级隔离；需要强边界时使用容器、Gondolin 或其他沙箱。

## 9. 扩展、Skills、提示词模板和 Pi package

### 9.1 扩展

扩展是使用 `jiti` 直接加载的 TypeScript/JavaScript 模块，拥有完整进程权限。主要 API：

- `pi.on()`：订阅并参与生命周期事件
- `pi.registerTool()`：注册或覆盖工具
- `pi.registerCommand()`：注册 slash command
- `pi.registerShortcut()` / `pi.registerFlag()`
- `pi.registerProvider()`：注册或覆盖模型 Provider
- `pi.sendMessage()` / `pi.sendUserMessage()`
- `pi.appendEntry()`：写入不进入模型上下文的扩展状态
- 注册消息、Entry 和 TUI 渲染器
- 动态改变活动工具、模型和思考级别

关键事件覆盖：

- 项目信任与资源发现
- Session 启动、切换、fork、压缩、树导航和关闭
- Agent、turn、message、tool 生命周期
- 输入、上下文、Provider headers/payload/response
- 模型和思考级别选择

全局扩展位于 `~/.pi/agent/extensions/`，项目扩展位于 `.pi/extensions/`，项目扩展只在项目可信后加载。

### 9.2 Skills

Skills 遵循 Agent Skills 规范，采用渐进披露：

1. 启动时只读取名称和描述。
2. 系统提示词只放可用 Skill 索引。
3. 模型匹配到任务后使用 `read` 读取完整 `SKILL.md`。
4. `/skill:name` 可以强制把 Skill 正文展开进用户消息。

当 `read` 工具未启用时，Skill 索引不会加入系统提示词。`disable-model-invocation: true` 的 Skill 也不会进入索引，只允许用户显式调用。

### 9.3 其他资源

- Prompt templates：作为 slash command 展开为用户提示词。
- Themes：控制交互 TUI 配色。
- `AGENTS.md`/`CLAUDE.md`：从全局目录和当前目录祖先链加载到系统提示词。
- `SYSTEM.md`：替换默认系统提示词。
- `APPEND_SYSTEM.md`：追加系统提示词。
- Pi package：通过 npm、git 或本地路径打包扩展、Skills、prompts 和 themes。

[`DefaultResourceLoader`](packages/coding-agent/src/core/resource-loader.ts) 统一完成发现、来源标记、碰撞诊断、项目可信过滤和重载。

### 9.4 MCP

仓库没有内置 MCP 客户端或 Server 集成。需要 MCP 时，应：

- 安装或编写增加 MCP 支持的扩展；或
- 将外部能力包装为 CLI，再通过 Skill 指导模型使用。

## 10. 系统提示词构成

默认系统提示词由 [`packages/coding-agent/src/core/system-prompt.ts`](packages/coding-agent/src/core/system-prompt.ts) 构建，主要包含：

1. Agent 角色说明
2. 当前活动工具及工具提供的简短说明
3. 工具提供的额外使用准则
4. Pi 自身文档入口
5. 全局及项目上下文文件
6. Skill 索引
7. 当前工作目录

扩展可以在每次请求前通过 `before_agent_start` 修改系统提示词；活动工具发生变化时，基础系统提示词也会重建。

## 11. 会话、树与上下文压缩

### 11.1 当前 CLI：JSONL Session

当前 CLI 会话默认保存在 `~/.pi/agent/sessions/`，每个会话是一个 JSONL 文件。

第一行是 Session header，其余 Entry 通过 `id/parentId` 形成树。主要 Entry：

- 消息
- 模型和思考级别变更
- 压缩摘要
- 分支摘要
- 扩展自定义状态和自定义上下文消息
- 标签和 Session 名称

`/tree` 在同一文件内切换活动叶节点；`/fork` 和 `/clone` 创建新 Session 文件。

### 11.2 上下文压缩

压缩不会简单删除整个历史，而是写入一个 `compaction` Entry：

- `summary` 保存早期上下文摘要
- `retainedTail` 或兼容字段保存应继续保留的最近上下文
- 后续构建模型上下文时从该压缩检查点继续

触发方式：

- 用户手动 `/compact`
- 上下文达到阈值
- Provider 返回上下文溢出

切换树分支时还可为放弃的分支生成 `branch_summary`，将旧分支的重要信息带到新位置。

### 11.3 新 Harness：通用 Session 后端

新 `packages/agent/src/harness/session/` 把 Session 抽象为 repo/storage，提供内存和 JSONL 实现；`packages/storage/sqlite-node` 提供 WAL 模式的 SQLite 实现。

这套存储服务于新 `AgentHarness`，不是当前 CLI `SessionManager` 的替代配置项。

## 12. TUI 与运行模式

### 12.1 `pi-tui`

[`packages/tui`](packages/tui/) 是独立终端 UI 框架，特点包括：

- Component/Container 组合
- 差量渲染和同步输出，降低闪烁
- Editor、Input、Markdown、SelectList、SettingsList 等组件
- Kitty/iTerm2 图片协议
- 键盘解析、自动补全、IME 光标定位
- 可定位、可堆叠的 Overlay

### 12.2 Coding Agent 模式

- `InteractiveMode`：把 `AgentSession` 事件渲染为完整对话 TUI。
- `runPrintMode`：适合一次性命令、管道和 JSON event stream。
- `runRpcMode`：通过 stdin/stdout 一行一个 JSON 对象交互，适合其他程序嵌入。
- SDK：直接调用 `createAgentSession()`，复用同一 AgentSession。

## 13. Server 包

`packages/server` 仍标记为 Experimental。

其核心模式是：

1. 本地 IPC Server 接受实例管理请求。
2. 每个实例启动独立的 `pi --mode rpc` 子进程。
3. `ServerSupervisor` 保存实例状态，转发 RPC、Agent 事件和扩展 UI 请求。
4. Server 重启时将遗留在线实例标记为停止，不尝试恢复进程。
5. 可选接入 Radius 做机器和 Pi 实例 presence。

不要把该包当成稳定的远程多租户服务框架。

## 14. 开发、测试与发布

### 14.1 基本命令

```bash
npm install --ignore-scripts
npm run check
./test.sh
./pi-test.sh
```

- `npm run check` 会执行 Biome 格式化/检查、依赖固定检查、TypeScript import 检查、shrinkwrap/install lock 校验、类型检查和浏览器 smoke check。
- `./test.sh` 在隔离的 HOME、临时目录和无 API key 环境中运行非 E2E 测试。
- `./pi-test.sh` 从源码启动 CLI，并保留调用者当前工作目录。

修改代码时必须遵守 [`AGENTS.md`](AGENTS.md) 中更具体的命令约束，特别是测试范围、依赖安装方式和禁止擅自提交。

### 14.2 技术约束

- Node.js `>=22.19.0`
- ESM
- TypeScript strict mode
- 根配置要求 erasable TypeScript syntax
- 内部源码使用 `.ts` 相对 import，构建时改写为 `.js`
- 直接外部依赖固定精确版本
- 所有 package 锁步版本；当前为 `0.81.1`

### 14.3 测试分布

测试主要集中在：

- `pi-ai`：Provider 兼容性、认证、流式协议和模型行为
- `pi-agent-core`：Agent 循环、工具和新 Harness
- `pi-coding-agent`：Session、扩展、工具、CLI 和大量回归用例
- `pi-tui`：渲染、终端输入、宽度和组件行为

Server 和 SQLite 包当前没有同等规模的独立测试目录，是理解其成熟度时需要注意的信号。

### 14.4 发布

所有 package 锁步发版。根 release 脚本负责：

- 统一提升版本
- 更新 changelog 和锁文件
- 构建并检查
- 创建 release commit 和 tag
- 推送后由 GitHub Actions 发布 npm package 和独立二进制

具体发布流程必须以 [`AGENTS.md`](AGENTS.md) 的 Releasing 章节为准。

## 15. 设计取舍与阅读时的常见误区

### 15.1 核心小，扩展面大

Pi 的首要产品取舍不是“内置功能最多”，而是“核心容易理解，功能可以替换”。新增工作流通常应优先考虑扩展或 Skill，而不是直接加进核心。

### 15.2 项目信任不是沙箱

项目可信状态决定是否加载项目本地可执行资源，但不会限制已加载代码、工具或 shell 的操作权限。

### 15.3 `Agent` 与 `AgentSession` 职责不同

- `Agent`：低层状态、事件、模型循环和工具执行。
- `AgentSession`：Coding Agent 产品编排，包括持久化、扩展、重试、压缩、模型管理和 Bash 消息。

产品行为问题多数应先查 `AgentSession`，模型循环问题才先查 `Agent`/`agent-loop`。

### 15.4 不要混淆两套 Session

- 当前 CLI：`packages/coding-agent/src/core/session-manager.ts`
- 新 Harness：`packages/agent/src/harness/session/`
- SQLite：仅属于新 Harness Session 后端

### 15.5 设计文档可能包含未完成项

尤其是 `packages/agent/docs/agent-harness.md`、`hooks.md`、`durable-harness.md` 和 `observability.md`。引用其中结论前，要再检查源码和测试是否已实现。

## 16. 按问题定位代码

| 问题 | 首选入口 |
|---|---|
| CLI 启动、参数、模式选择 | `packages/coding-agent/src/main.ts`、`src/cli/args.ts` |
| 一次 prompt 为什么这样执行 | `core/agent-session.ts` → `packages/agent/src/agent.ts` → `agent-loop.ts` |
| Provider、认证或流式兼容 | `packages/ai/src/models.ts`、`src/providers/`、`src/api/` |
| CLI 模型选择和自定义 Provider | `core/model-runtime.ts`、`model-config.ts`、`provider-composer.ts` |
| 工具行为 | `core/tools/`，再看 `agent-loop.ts` 的工具调度 |
| 扩展事件或 API | `core/extensions/types.ts`、`runner.ts`、`loader.ts` |
| Skill/模板/主题/上下文发现 | `core/resource-loader.ts`、`package-manager.ts` |
| 当前 CLI Session、树和恢复 | `core/session-manager.ts`、`core/compaction/` |
| 新 Agent Harness | `packages/agent/src/harness/agent-harness.ts` |
| 新 Session 后端 | `packages/agent/src/harness/session/`、`packages/storage/sqlite-node/` |
| 交互界面 | `modes/interactive/interactive-mode.ts`、`packages/tui/src/tui.ts` |
| 外部进程集成 | `modes/rpc/`、`packages/server/src/` |

## 17. 推荐阅读顺序

第一次理解项目，建议按以下顺序：

1. [`README.md`](README.md)
2. [`packages/coding-agent/docs/index.md`](packages/coding-agent/docs/index.md)
3. [`packages/coding-agent/src/main.ts`](packages/coding-agent/src/main.ts)
4. [`packages/coding-agent/src/core/sdk.ts`](packages/coding-agent/src/core/sdk.ts)
5. [`packages/coding-agent/src/core/agent-session.ts`](packages/coding-agent/src/core/agent-session.ts)
6. [`packages/agent/README.md`](packages/agent/README.md)
7. [`packages/agent/src/agent-loop.ts`](packages/agent/src/agent-loop.ts)
8. [`packages/ai/src/models.ts`](packages/ai/src/models.ts)
9. [`packages/coding-agent/src/core/resource-loader.ts`](packages/coding-agent/src/core/resource-loader.ts)
10. [`packages/coding-agent/docs/extensions.md`](packages/coding-agent/docs/extensions.md)
11. [`packages/coding-agent/docs/session-format.md`](packages/coding-agent/docs/session-format.md)
12. 最后再读新 Harness 的 [`packages/agent/docs/agent-harness.md`](packages/agent/docs/agent-harness.md)

## 18. 给新 AI 对话的最短上下文

可以直接对新 AI 说：

> 先完整阅读仓库根目录的 `AGENTS.md` 和 `项目导览.md`。当前 pi CLI 由 `AgentSessionRuntime → AgentSession → Agent → agent-loop → pi-ai Provider` 驱动，Session 使用 coding-agent 的 JSONL `SessionManager`。`packages/agent/src/harness/` 和 `packages/storage/sqlite-node/` 属于正在演进的新通用 Harness，不是当前 CLI 主链。修改前先明确目标属于当前 CLI、低层 Agent、模型层还是新 Harness，并以源码和测试确认设计文档中的能力是否已经落地。
